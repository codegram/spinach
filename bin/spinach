#!/usr/bin/env ruby

begin
  require 'spinach'
rescue LoadError
  require_relative '../lib/spinach'
end

if ARGV.any?
  Spinach::Runner.new(ARGV).run
else
  Spinach::Runner.new(Dir.glob(File.join 'features', '**', '*.feature')).run
end
